{
  "name": "custom-custom-three-engine",
  "version": "0.0.3",
  "description": "",
  "private": false,
  "license": "ISC",
  "author": "Rob Doneux",
  "type": "commonjs",
  "files": [
    "dist"
  ],
  "main": "./dist/custom-three-engine.umd.cjs",
  "module": "./dist/custom-three-engine.js",
  "types": "./dist/main.d.ts",
  "publishConfig": {
    "registry": "http://192.168.0.106:4873"
  },
  "sideEffects": false,
  "scripts": {
    "dev": "npm run build && concurrently \"npx ts-node ./build.ts --watch\" \"npx serve ./dist\"",
    "build": "rm -rf ./dist && tsc -b && npx ts-node ./build.ts",
    "publish": "npm run build && npm publish"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@types/three": "^0.172.0",
    "concurrently": "^9.1.2",
    "esbuild": "^0.24.2",
    "esbuild-plugin-copy": "^2.1.1",
    "typescript": "^5.7.3"
  }
}
